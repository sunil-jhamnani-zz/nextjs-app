const initialState = {
  fullName: 'Sunil Jhamnani',
  email: 'jhamnanisunil@gmail.com'
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}